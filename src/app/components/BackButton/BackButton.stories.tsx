import React from 'react';
import BackButton from './BackButton';

export default {
  title: 'Component/BackButton',
  component: BackButton,
};

export const BackArrow = (): JSX.Element => <BackButton />;
